When we are dealing with huge inputs even the smallest change to C(op) can impact hugely. When we use arrays to 
store the prime factors it takes forever. But when we simply print them on the screen it works instantly even
for huge inputs.